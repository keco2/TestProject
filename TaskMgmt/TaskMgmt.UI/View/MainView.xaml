<Window x:Class="TaskMgmt.UI.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskMgmt.UI.View"
        mc:Ignorable="d"
        Title="Task Management Software" Height="450" Width="800" Background="Beige">
    <TabControl Margin="10" SelectedIndex="{Binding SelectedTabIndex}">
        <TabItem Header="Tasks">
            <local:TaskControl DataContext="{Binding TaskDataContext}" />
        </TabItem>
        <TabItem Header="Usages">
            <local:UsageControl DataContext="{Binding UsageDataContext}" />
        </TabItem>
    </TabControl>
</Window>
